# Waveshare USB-CAN Bridge Configuration
# Copy this file to .env and customize for your setup
# Note: Tests will also use this configuration if .env exists, or default to /dev/ttyUSB0

# SocketCAN interface name (e.g., can0, vcan0)
# For testing: vcan0 (virtual CAN interface)
WAVESHARE_SOCKETCAN_INTERFACE=vcan0

# USB device path
# For testing: Point this to your USB-CAN adapter (e.g., /dev/ttyUSB0)
WAVESHARE_USB_DEVICE=/dev/ttyUSB0

# Serial baud rate (bps)
# Supported: 9600, 19200, 38400, 57600, 115200, 153600, 2000000
WAVESHARE_SERIAL_BAUD=2000000

# CAN bus baud rate (bps)
# Supported: 5000, 10000, 20000, 50000, 100000, 125000, 200000, 250000, 400000, 500000, 800000, 1000000
WAVESHARE_CAN_BAUD=1000000

# CAN mode
# Options: normal, loopback, silent, loopback-silent
WAVESHARE_CAN_MODE=normal

# Automatic retransmission
# Options: true, false, 1, 0, yes, no
WAVESHARE_AUTO_RETRANSMIT=true

# CAN filter ID (hex or decimal)
# Example: 0x123 or 291
WAVESHARE_FILTER_ID=0x0

# CAN filter mask (hex or decimal)
# Example: 0x7FF or 2047
WAVESHARE_FILTER_MASK=0x0

# USB read timeout (milliseconds)
WAVESHARE_USB_READ_TIMEOUT=100

# SocketCAN read timeout (milliseconds)
WAVESHARE_SOCKETCAN_READ_TIMEOUT=100
